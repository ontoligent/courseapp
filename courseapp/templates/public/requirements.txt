# CI installs the courseapp tool (the MkDocs toolchain comes in transitively).
# Points at the published courseapp repo so GitHub Actions can install it.
# Local builds use your pipx-installed courseapp instead.
__COURSEAPP_REF__
